export type IUser = {
    email: string,
    password: string
}

export type IUserList = {
    email: number;
    password: string;
};

export type IUserRegister = {
    username: string;
    password: string;
    email: string;
    firstName: string;
    lastName: string;
}